# Optional: Google Analytics 4 measurement ID (e.g. G-XXXXXXXXXX).
# Leave unset and no analytics scripts load — handy for local dev and forks.
# Set it in your deploy environment to switch tracking on.
NEXT_PUBLIC_GA_ID=
